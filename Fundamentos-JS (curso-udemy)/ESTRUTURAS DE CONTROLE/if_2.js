const nota = 8;
const bomComportamento = true;

if (nota >= 7 && bomComportamento)
// && = E/ AND;
    console.log('Parabéns!');

if (nota <7 || !bomComportamento)
// ||= OU/OR... != NOT; 
    console.log ("Que pena :(");

console.log ("Fim!")
