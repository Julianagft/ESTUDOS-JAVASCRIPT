const hora = 14;
const saudacao = hora <= 11 ? "Bom dia!" : "Boa tarde" ; //Vai ser sempre composto por esses três elementos;
//se a informação for verdadeira o resultado será a primeira string, por isso a ordem delas é importante;

console.log (saudacao);
 