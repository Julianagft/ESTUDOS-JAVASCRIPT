const numeros = [1, 2, 3, 4, 5, 6, 11, 45];

for (let i = 0; i < numeros.length; i++) { //o 'numeros.length' trás flexibilidade para o laço de array; 
     console.log (numeros [i]);

}

for (let i = 0; i < numeros.length; i+= 2) { //Fiz ele pular de dois em dois; 
    console.log (numeros [i]);

}